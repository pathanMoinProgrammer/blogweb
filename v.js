// const array = [1, 4, 3, 3, 1, 4, 5, 6];
// let newArray = [];

// for (let item of array) {
//   if (!newArray.includes(item)) {
//     newArray.push(item);
//   }
// }

// for (let i = 1; i < newArray.length; i++) {
//   if (newArray[i] < newArray[i - 1]) {
//     let temp = newArray[i];
//     newArray[i] = newArray[i - 1];
//     newArray[i - 1] = temp;
//   }
// }

// console.log(newArray);

// 'use client';
// import React, { useState } from 'react';

// const page = () => {
//   const [state, seState] = useState(2);
//   return (
//     <div>
//       <ACom state={state} />
//       <BCom setData={seState} />
//     </div>
//   );
// };

// const ACom = ({ state }) => {
//   return <div>{state} aa</div>;
// };

// const BCom = ({ setData }) => {
//   return <button onClick={() => setData((prev) => prev + 1)}>Click</button>;
// };

// export default page;


// ```and see, it was error of seo, so resolve this alao "Add a title to your site." and "Add internal links for a better usability and so bots can reach all areas of your website more quickly. " and "Use 301 redirects to drive traffic to URLS with the same domain and sub domain (www and non-www subdomain). " adn "Add a meta description. " and "Use only one H1 heading on the page. " and "Try to reduce the number of used JavaScript files. " and "Words from H1 heading not found in text. Make sure that H1 content matches page content and vice versa. " and "The title tag is missing." and "The meta description is missing" and "There are only 33 words on this page. Use at least 850 words to provide useful information" and "Words from the H1 heading are not used in the page content." and "No paragraphs were detected." and "No Apple touch icon is specified" and "This page loads 12 JavaScript files. This may affect the load time negatively" and "There are few social sharing options on the page. Sharing plugins can increase the page's reach on social media." and "Too many H1 headings are used." and "This page is a dead end, because there are no internal links." and "This website uses URLs with www and non-www subdomain. This may cause duplicate content and bad links to your website. Redirect Checker" and "The X-powered header is sent within the response header. & The web server version is sent within the HTTP header" and "This page loads 12 JavaScript files. This may affect the load time negatively" and "We didn't find any backlinks to this page." please plaese see and explore everything in blogweb, then apply changes only in blogweb, so do carefully and my ofiicial site which on this website was live is "https://explorethebuzz.com/"```






let str = "Hello World";

console.log(str.split(0, 3))  
// console.log(str.slice(0, 3))  